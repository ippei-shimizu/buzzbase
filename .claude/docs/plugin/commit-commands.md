# commit-commands — Git操作の自動化（smart-commit の後継）

```
/commit              # 差分分析 → コミットメッセージ自動生成 → コミット
/commit-push-pr      # コミット → プッシュ → PR作成を一括実行
/clean_gone          # リモートで削除済みのローカルブランチを一括掃除
```

**注意:** CLAUDE.md のコミットルール（日本語、`[Type]: [説明]` 形式）が優先される。
